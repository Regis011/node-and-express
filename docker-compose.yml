version: '3.9'
services:
  app:
    build: .
    ports:
      - '5000:5000'
    env_file:
      - .env
    volumes:
      - .:/usr/src/app
    depends_on:
      - mongo

  mongo:
    image: mongo:7
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
